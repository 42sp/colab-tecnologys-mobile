import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function SignUp() {
    return (
        <SafeAreaView>
            <Text className="flex self-center text-xl">Sign-Up</Text>
        </SafeAreaView>
    )
}
